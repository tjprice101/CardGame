import type { CardDefinition } from '@/types/cards';

export function isDeathFlamedHellBaseDefinitionId(_definitionId: string): boolean {
  return false;
}

export function isDeathFlamedHellBaseCard(_card?: CardDefinition | null): boolean {
  return false;
}
